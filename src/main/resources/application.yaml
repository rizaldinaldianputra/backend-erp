spring:
  application:
    name: erp
  datasource:
    url: jdbc:postgresql://localhost:5432/erp
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password
  mail:
    host: smtp.gmail.com
    port: 587
    username: skycosmicfoundation@gmail.com
    password: otrgwkgmrgtcaref
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update  # update schema tanpa hapus data lama
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

camunda:
  bpm:
    auto-deployment-enabled: true
    database:
      schema-update: true        # auto-create Camunda tables in PostgreSQL
      type: postgres
    admin-user:
      id: admin
      password: admin
      first-name: Admin
    filter:
      create: All Tasks          # create default filter in Tasklist UI
    history-level: full

spring.devtools:
  restart:
    enabled: true # otomatis restart saat ada perubahan kode
  livereload:
    enabled: true # reload browser jika pakai web

minio:
  url: http://localhost:9000
  access.key: minioadmin
  secret.key: minioadmin
  bucket.name: skyerp-assets
